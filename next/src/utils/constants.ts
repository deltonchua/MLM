export const ADDRESS_PATTERN = /^0x[a-fA-F0-9]{40}$/;

export const NAME_PATTERN = /^.{4,40}$/;

export const DISPLAY_NAME_PATTERN = /^[a-zA-Z0-9]{4,20}$/;

export const EMAIL_PATTERN = /^[^@ \t\r\n]+@[^@ \t\r\n]+\.[^@ \t\r\n]+$/;

export const AUTOID_PATTERN = /^[A-Za-z0-9]{20}$/;

export const IMG_EXTENSION = ['.gif', '.jpg', '.jpeg', '.png', '.svg'];

export const MAX_FILE_SIZE = 1_000_000;

export const MIN_PAYOUT = 0.2;
